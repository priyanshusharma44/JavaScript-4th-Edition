// 2. Create an array of strings and use slice() to extract a portion of the array from index 1 to index 3 (inclusive). Print the extracted portion.
const cars = ["bugati","ferari","audi","nexion","volkswagen"];

const newCars = cars.slice(1,4);
console.log(newCars);