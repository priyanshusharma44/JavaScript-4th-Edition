// 5. Create an array of numbers and use slice() to create a new array containing the last three elements. Print the new array.

const number = [5,74,7,3,9,2,0,8];

const newNumber = number.slice(5,number.length);
console.log(newNumber);