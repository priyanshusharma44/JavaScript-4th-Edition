//Create an empty array and use push() to add the numbers 1 through 5 to the array. Then, use pop() to remove the last two elements from the array.
const arr =[];
arr.push(1);
arr.push(2);
arr.push(3);
arr.push(4);
arr.push(5);
console.log(arr);

arr.pop();
arr.pop();

console.log(arr);